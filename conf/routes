# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /messages/:username         controllers.MessagesController.getMessages(username)
POST    /messages                   controllers.MessagesController.sendMessage

